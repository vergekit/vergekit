---
import AuthShell from '@/components/auth/AuthShell.astro';
import { KeyRoundIcon } from '@lucide/astro';
import { Field, FieldLabel } from '@/components/ui/field';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';

const token = Astro.url.searchParams.get('token') ?? '';
---

<AuthShell
  title="Choose a new password"
  eyebrow="Account recovery"
  description="Set a new password for your account."
>
  <div class="mb-8 hidden lg:block">
    <h2 class="text-2xl font-semibold tracking-normal text-slate-950">
      New password
    </h2>
    <p class="mt-2 text-sm leading-6 text-slate-600">
      Set the replacement password for your account.
    </p>
  </div>

  <form
    method="post"
    action="/api/auth/reset-password"
    class="space-y-5"
    data-auth-form
    data-auth-error="reset-password-error"
    data-auth-success-url="/login?passwordReset=1"
  >
    <input type="hidden" name="token" value={token} />

    <p
      id="reset-password-error"
      class="rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-700"
      role="alert"
      aria-live="polite"
      hidden
    />

    <Field>
      <FieldLabel for="new-password">New password</FieldLabel>
      <Input
        id="new-password"
        name="newPassword"
        type="password"
        autocomplete="new-password"
        minlength="8"
        required
      />
    </Field>

    <Button type="submit" class="w-full">
      <KeyRoundIcon aria-hidden="true" />
      Update password
    </Button>
  </form>

  <p class="mt-6 text-center text-sm text-slate-600">
    Need a fresh link? &nbsp;
    <a
      href="/auth/forgot-password"
      class="font-medium text-slate-950 hover:text-emerald-700"
    >
      Request one
    </a>
  </p>
</AuthShell>
