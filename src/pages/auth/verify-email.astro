---
import AuthShell from '@/components/auth/AuthShell.astro';
import Button from '@/components/ui/Button.astro';
import Field from '@/components/ui/Field.astro';
import Icon from '@/components/ui/Icon.astro';
import Input from '@/components/ui/Input.astro';
import { appConfig } from '@/config/app';
---

<AuthShell
  title="Verify your email"
  eyebrow="Confirm ownership"
  description="Confirm the address tied to your account."
>
  <div class="mb-8 hidden lg:block">
    <h2 class="text-2xl font-semibold tracking-normal text-slate-950">
      Email verification
    </h2>
    <p class="mt-2 text-sm leading-6 text-slate-600">
      Send a fresh verification message.
    </p>
  </div>

  <form
    method="post"
    action="/api/auth/send-verification-email"
    class="space-y-5"
  >
    <input
      type="hidden"
      name="callbackURL"
      value={appConfig.defaultAuthenticatedPath}
    />

    <Field id="verify-email" label="Email" required>
      <Input
        id="verify-email"
        name="email"
        type="email"
        autocomplete="email"
        placeholder="you@example.com"
        required
      />
    </Field>

    <Button type="submit" fullWidth>
      <Icon name="badge-check" size={18} aria-hidden="true" />
      Send verification
    </Button>
  </form>

  <p class="mt-6 text-center text-sm text-slate-600">
    Already verified?
    <a href="/login" class="font-medium text-slate-950 hover:text-emerald-700">
      Sign in
    </a>
  </p>
</AuthShell>
